import HomePageServer from "@/components/server/HomePageServer";

export const metadata = {
  title: "MyShaadiStore | Plan Your Dream Wedding",
  description: "A guided wedding planning journey powered by admin-managed steps, vendors, and curated items.",
};

export default function HomePage() {
  return <HomePageServer />;
}
